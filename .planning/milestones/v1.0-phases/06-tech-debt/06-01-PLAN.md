---
phase: 06-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server.js]
autonomous: true
requirements: [STAB-02, CLN-03]

must_haves:
  truths:
    - "cache.finalized is initialized inside the cache object literal at line ~166, not deferred to line 783"
    - "SHORT_MONTHS does not appear in the server.js require/destructure block"
    - "DIAG_FIXED_TEMPLATE is not defined as a const in server.js — it is imported from lib/business-logic.js"
  artifacts:
    - path: "server.js"
      provides: "Clean imports, single-source template, co-located cache init"
      contains: "DIAG_FIXED_TEMPLATE"
  key_links:
    - from: "server.js"
      to: "lib/business-logic.js"
      via: "require destructure"
      pattern: "DIAG_FIXED_TEMPLATE.*require.*business-logic"
    - from: "server.js getDiagTemplateWarnings()"
      to: "DIAG_FIXED_TEMPLATE"
      via: "imported reference"
      pattern: "DIAG_FIXED_TEMPLATE\\.arkbar"
---

<objective>
Fix three tech debt items identified by the v1.0 milestone audit: move cache.finalized into the cache object literal, remove the unused SHORT_MONTHS import, and replace the local DIAG_FIXED_TEMPLATE copy with an import from lib/business-logic.js.

Purpose: Eliminate structural fragility (cache init order), dead code (unused import), and duplication risk (template drift).
Output: A cleaner server.js with no behavioral changes.
</objective>

<execution_context>
@C:/Users/gusno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- From server.js lines 166-170: cache object literal -->
const cache = {
  djs:          { data: null, time: 0, ttl: 10 * 60 * 1000 },
  availability: new Map(),
  roster:       new Map(),
};
<!-- cache.finalized assigned separately at line 783 -->
cache.finalized = { data: null, time: 0, ttl: 5 * 60 * 1000 };

<!-- From server.js lines 112-123: current import block -->
const {
  normalizeSlot, pad2, makeDateKey, parseDateKey,
  RESIDENTS, ALL_SLOTS, MONTH_NAMES,
  SHORT_MONTHS,          // <-- unused in server.js body
  FIXED_SCHEDULES,
  buildAvailabilityMap, computeFinalizationReport, getDJTemplateBlocks,
} = require('./lib/business-logic');

<!-- From lib/business-logic.js line 310: DIAG_FIXED_TEMPLATE is exported -->
module.exports = { ..., DIAG_FIXED_TEMPLATE, ... };

<!-- From server.js lines 439-470: local DIAG_FIXED_TEMPLATE copy (to be removed) -->
<!-- Used by getDiagTemplateWarnings() at line 475 and diagnostic endpoint at lines 615-626 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move cache.finalized into cache literal and remove unused SHORT_MONTHS import</name>
  <files>server.js</files>
  <action>
Two surgical edits to server.js:

1. CACHE INIT: Add `finalized` to the cache object literal (line ~166-170) so it reads:
```js
const cache = {
  djs:          { data: null, time: 0, ttl: 10 * 60 * 1000 },
  availability: new Map(),
  roster:       new Map(),
  finalized:    { data: null, time: 0, ttl: 5 * 60 * 1000 },
};
```
Then DELETE the standalone `cache.finalized = ...` line at ~783. The line should be completely removed (not commented out).

2. DEAD IMPORT: Remove `SHORT_MONTHS,` from the destructure block at line ~118. Leave all other imports intact. Ensure no trailing comma issues after removal (the line before or after should still have proper comma syntax).

IMPORTANT: These are refactors with ZERO behavioral change. Do not modify any logic, only move/remove declarations.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('server.js','utf8'); const cacheMatch=s.match(/const cache = \{[^}]+finalized[^}]+\}/s); const deferredMatch=s.match(/^cache\.finalized\s*=/m); const shortMatch=s.match(/SHORT_MONTHS/); if(!cacheMatch) process.exit(1); if(deferredMatch) { console.error('FAIL: deferred cache.finalized still exists'); process.exit(1); } if(shortMatch) { console.error('FAIL: SHORT_MONTHS still referenced'); process.exit(1); } console.log('PASS: cache.finalized in literal, no deferred assign, no SHORT_MONTHS');"</automated>
  </verify>
  <done>cache.finalized is in the cache object literal; standalone assignment at line ~783 is gone; SHORT_MONTHS is not imported or referenced in server.js</done>
</task>

<task type="auto">
  <name>Task 2: Replace local DIAG_FIXED_TEMPLATE with import from lib/business-logic.js</name>
  <files>server.js</files>
  <action>
1. ADD `DIAG_FIXED_TEMPLATE` to the destructure in the require('./lib/business-logic') block (line ~112-123). Place it after `FIXED_SCHEDULES` for logical grouping with other constants. The updated block should include:
```js
const {
  normalizeSlot, pad2, makeDateKey, parseDateKey,
  RESIDENTS, ALL_SLOTS, MONTH_NAMES,
  FIXED_SCHEDULES,
  DIAG_FIXED_TEMPLATE,
  buildAvailabilityMap, computeFinalizationReport, getDJTemplateBlocks,
} = require('./lib/business-logic');
```

2. DELETE the entire local `const DIAG_FIXED_TEMPLATE = { ... };` block (lines ~439-470). This is approximately 32 lines defining the love/arkbar/hip template object.

3. UPDATE the comment at line ~437 that says "Server-side copy of FIXED_TEMPLATE from roster.html" — either remove it entirely or replace with a brief note like:
```js
// DIAG_FIXED_TEMPLATE imported from lib/business-logic.js (single source of truth).
```

4. KEEP the comment at line ~507-508 that says "server.js retains DIAG_FIXED_TEMPLATE locally for getDiagTemplateWarnings()" — update it to say "DIAG_FIXED_TEMPLATE imported from lib/business-logic.js; used by getDiagTemplateWarnings() and diagnostic endpoint below."

5. DO NOT modify getDiagTemplateWarnings() or the diagnostic endpoint logic — they already reference DIAG_FIXED_TEMPLATE by name, which will now resolve to the import.

IMPORTANT: Zero behavioral change. The imported object is identical to the local copy (both were created during Phase 5 extraction from the same source).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('server.js','utf8'); const importMatch=s.match(/DIAG_FIXED_TEMPLATE[^}]*\}\s*=\s*require\(.+business-logic/s); const localDef=s.match(/^const DIAG_FIXED_TEMPLATE\s*=/m); if(!importMatch) { console.error('FAIL: DIAG_FIXED_TEMPLATE not in import block'); process.exit(1); } if(localDef) { console.error('FAIL: local DIAG_FIXED_TEMPLATE const still exists'); process.exit(1); } console.log('PASS: DIAG_FIXED_TEMPLATE imported, no local definition');" && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>DIAG_FIXED_TEMPLATE exists in one canonical location (lib/business-logic.js) and server.js imports it; no local copy remains; all existing tests pass</done>
</task>

</tasks>

<verification>
All three tech debt items resolved:
1. `grep -n "finalized" server.js` shows finalized ONLY inside the cache literal and in usage sites (invalidateCaches, fetchFinalized) — no standalone assignment
2. `grep -n "SHORT_MONTHS" server.js` returns zero matches
3. `grep -n "DIAG_FIXED_TEMPLATE" server.js` shows it in the import destructure and usage sites only — no `const DIAG_FIXED_TEMPLATE =` definition
4. `node server.js &` starts without error (kill after confirming startup)
5. `npx jest` — all existing tests pass
</verification>

<success_criteria>
- cache.finalized initialized in the cache object literal (co-located with djs, availability, roster)
- No unused imports in the require/destructure from lib/business-logic.js
- DIAG_FIXED_TEMPLATE defined in exactly one place (lib/business-logic.js), imported by server.js
- Server starts without errors
- All 49 Jest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt/06-01-SUMMARY.md`
</output>
