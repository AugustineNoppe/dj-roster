---
phase: 01-auto-suggest
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - lib/roster-logic.js
  - lib/roster-logic.test.js
  - package.json
autonomous: true
requirements:
  - ASGN-02
  - ASGN-03

must_haves:
  truths:
    - "canAssign() and block-enforcement logic are importable from a shared module"
    - "Jest test with fixture availability data asserts zero unavailability violations"
    - "Jest test with fixture availability data asserts zero partial blocks"
    - "All Jest tests pass"
  artifacts:
    - path: "lib/roster-logic.js"
      provides: "Extracted canAssign(), getUnavailLookupDate(), and block-enforcement logic"
      exports: ["canAssign", "getUnavailLookupDate", "assignBlocks"]
    - path: "lib/roster-logic.test.js"
      provides: "Jest tests asserting no violations and no partial blocks"
      contains: "expect.*0"
    - path: "package.json"
      provides: "Jest dev dependency and test script"
      contains: "jest"
  key_links:
    - from: "lib/roster-logic.test.js"
      to: "lib/roster-logic.js"
      via: "require/import of canAssign, assignBlocks"
      pattern: "require.*roster-logic"
---

<objective>
Extract canAssign() and block-enforcement logic into a shared module and write Jest tests that assert no unavailability violations and no partial blocks with fixture data.

Purpose: Per user decision in CONTEXT.md, automated Jest tests are required to verify the auto-suggest fix. Extracting the logic into a shared module makes it testable outside the browser and provides regression protection.

Output: Shared roster-logic.js module with Jest test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auto-suggest/01-CONTEXT.md
@.planning/phases/01-auto-suggest/01-02-SUMMARY.md
@public/roster.html
@package.json

<interfaces>
<!-- Functions to extract from roster.html after Plan 02 fixes are applied -->

From roster.html -- canAssign (lines 1251-1257):
```javascript
function canAssign(dj, dateStr, slot, targetMap) {
  if (dj !== 'Guest DJ' && submittedDJs.get(dj) !== 'submitted') return false;
  const unavailDate = getUnavailLookupDate(dateStr, slot);
  if (unavailabilityMap[dj]?.includes(`${unavailDate}|${slot}`) ) return false;
  if (targetMap[`${dateStr}|${slot}`]) return false;
  return true;
}
```

From roster.html -- getUnavailLookupDate (lines 1241-1248):
```javascript
function getUnavailLookupDate(dateStr, slot) {
  // Returns the previous calendar day for post-midnight slots (00:xx, 01:xx)
  // Returns the same date for all other slots
}
```

From roster.html -- block enforcement pattern (after Plan 02 fix):
```javascript
// Group template slots by DJ, then:
if (blockSlots.every(slot => canAssign(dj, dateStr, slot, targetMap))) {
  // assign all slots in block
} else {
  // skip entire block
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract roster logic into shared module and write Jest tests</name>
  <files>lib/roster-logic.js, lib/roster-logic.test.js, package.json</files>
  <behavior>
    - Test 1: canAssign returns false when DJ is marked unavailable for the given date+slot
    - Test 2: canAssign returns false when DJ has not submitted availability
    - Test 3: canAssign returns true when DJ is available and submitted
    - Test 4: canAssign handles post-midnight slots by checking previous calendar day
    - Test 5: assignBlocks assigns all slots in a block when all pass canAssign
    - Test 6: assignBlocks assigns zero slots when any slot in the block fails canAssign (no partial blocks)
    - Test 7: Full scenario -- given a fixture FIXED_TEMPLATE, unavailabilityMap, and submittedDJs, running assignBlocks produces zero unavailability violations
    - Test 8: Full scenario -- running assignBlocks produces zero partial blocks (every DJ gets their full template block or nothing)
  </behavior>
  <action>
1. Install Jest: add `jest` as a devDependency in package.json, add `"test": "jest"` script.

2. Create `lib/roster-logic.js` (CommonJS module) that exports:
   - `getUnavailLookupDate(dateStr, slot)` -- extracted from roster.html
   - `canAssign(dj, dateStr, slot, targetMap, unavailabilityMap, submittedDJs)` -- extracted from roster.html but with unavailabilityMap and submittedDJs as explicit parameters instead of closures
   - `assignBlocks(template, dateStr, canAssignFn, recordPlacementFn)` -- the block-grouped assignment logic from Plan 02's fix. Groups template slots by DJ, calls canAssignFn for every slot in the block, calls recordPlacementFn for all slots if all pass, skips entire block otherwise. Returns `{ assigned: [...], skipped: [...] }`.

3. Create `lib/roster-logic.test.js` with fixture data:
   - Fixture unavailabilityMap: include Raffo DJ unavailable on 2026-03-03 for slots that overlap with 11PM-2AM block
   - Fixture submittedDJs: Map with several DJs as 'submitted', one as 'not_submitted'
   - Fixture FIXED_TEMPLATE for ARKbar Tuesday: Raffo DJ at 23:00-00:00/00:00-01:00/01:00-02:00, Pick at 14:00-15:00/15:00-16:00/16:00-17:00
   - Test each behavior listed above
   - The full scenario tests should:
     a. Run assignBlocks with the fixture data
     b. Check every assignment against the unavailabilityMap -- assert zero violations
     c. Check every assigned DJ's slot count against their template block -- assert no partial blocks

4. Do NOT modify roster.html or server.js in this plan. The shared module is a parallel extractionfor testability. Roster.html continues to use its inline functions. A future plan can wire roster.html to import from this module if desired.
  </action>
  <verify>
    <automated>npx jest --verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lib/roster-logic.js exports canAssign, getUnavailLookupDate, and assignBlocks
    - lib/roster-logic.test.js has 8+ test cases covering all behaviors listed
    - All tests pass with `npx jest`
    - Zero unavailability violations asserted in full scenario test
    - Zero partial blocks asserted in full scenario test
    - Jest is installed as devDependency with "test" script in package.json
  </done>
</task>

</tasks>

<verification>
- `npx jest` passes all tests
- lib/roster-logic.js exports match the function signatures from roster.html
- Test fixtures include the known failing cases (Raffo DJ, Pick on Tue 3 March)
- No modifications to roster.html or server.js
</verification>

<success_criteria>
- Jest test suite passes with zero failures
- canAssign() logic matches roster.html behavior exactly
- assignBlocks() enforces all-or-nothing block assignment
- Full scenario test proves zero unavailability violations and zero partial blocks with realistic fixture data
</success_criteria>

<output>
After completion, create `.planning/phases/01-auto-suggest/01-03-SUMMARY.md`
</output>
