---
phase: 08-backend-server-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - lib/business-logic.test.js
autonomous: true
requirements: [SCHED-01, STAB-01, STAB-02]

must_haves:
  truths:
    - "fetchDJs() returns enriched DJ objects with type, recurringAvailability, and fixedSchedules from djs table"
    - "DJ login authenticates against djs.pin_hash instead of dj_pins table"
    - "Account lockout persists across server restarts (written to djs.failed_attempts and djs.locked_until)"
    - "All three lockout functions are async and hit Supabase in the same commit"
    - "Admin clear-lockout endpoint awaits the async clearFailedAttempts and has try-catch"
  artifacts:
    - path: "server.js"
      provides: "Enriched fetchDJs(), async lockout functions, auth against djs table"
      contains: "from('djs')"
    - path: "lib/business-logic.test.js"
      provides: "Test for buildAvailabilityMap with DB-shaped fixedSchedules (string keys)"
      contains: "fixedSchedules from DB"
  key_links:
    - from: "server.js:fetchDJs()"
      to: "supabase.from('djs')"
      via: "SELECT with active=true filter"
      pattern: "from\\('djs'\\).*select.*recurring_availability.*fixed_schedules"
    - from: "server.js:requireDJAuth()"
      to: "supabase.from('djs')"
      via: "SELECT pin_hash, failed_attempts, locked_until"
      pattern: "from\\('djs'\\).*select.*pin_hash"
    - from: "server.js:recordFailedAttempt()"
      to: "supabase.from('djs').update()"
      via: "UPDATE failed_attempts, locked_until"
      pattern: "from\\('djs'\\)\\.update.*failed_attempts"
---

<objective>
Switch the two foundational server functions (fetchDJs and requireDJAuth) from legacy tables to the djs table, and convert all lockout functions from in-memory Map to persistent DB calls.

Purpose: After this plan, the server reads all DJ data from the single djs table and lockout survives restarts. This is the prerequisite for all subsequent endpoint migrations in Plan 02.

Output: Modified server.js with enriched fetchDJs(), DB-backed auth, and persistent lockout. Updated tests covering DB-shaped fixedSchedules data.
</objective>

<execution_context>
@C:/Users/gusno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-server-cutover/08-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From server.js — current fetchDJs (lines 255-268) to be replaced:
```javascript
async function fetchDJs() {
  if (isFresh(cache.djs)) return cache.djs.data;
  const { data: ratesData, error: ratesError } = await supabase
    .from('dj_rates')
    .select('name, rate');
  if (ratesError) throw new Error(ratesError.message);
  const djs = (ratesData || []).map(({ name, rate }) => ({
    name, rate: parseInt(rate) || 0
  }));
  const result = { success: true, djs };
  cache.djs.data = result;
  cache.djs.time = Date.now();
  return result;
}
```

From server.js — current lockout functions (lines 77-105) to be replaced:
```javascript
const _loginAttempts = new Map();
const MAX_LOGIN_ATTEMPTS = 5;
const LOCKOUT_DURATION_MS = 15 * 60 * 1000;
function checkLockout(name) { /* reads from Map */ }
function recordFailedAttempt(name) { /* writes to Map */ }
function clearFailedAttempts(name) { /* deletes from Map */ }
```

From server.js — current requireDJAuth (lines 350-379) queries dj_pins table.
From server.js — /api/dj/login (lines 768-793) queries dj_pins table, uses RESIDENTS.includes().
From server.js — /api/admin/clear-lockout (lines 1220-1225) calls sync clearFailedAttempts.
From server.js — diagnostic endpoint line 648: `supabase.from('dj_rates').select('name')` — MUST retarget to djs.

djs table schema (confirmed Phase 7):
- id (uuid PK), name (text UNIQUE), pin_hash (text), rate (integer)
- type (text: 'resident'/'casual'/'guest'), active (boolean), venues (text[])
- recurring_availability (jsonb), fixed_schedules (jsonb)
- failed_attempts (integer), locked_until (timestamptz), created_at (timestamptz)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DB-shaped fixedSchedules test + switch fetchDJs to djs table</name>
  <files>lib/business-logic.test.js, server.js</files>
  <behavior>
    - Test: buildAvailabilityMap with DB-shaped fixedSchedules (string keys like "1", "3") produces identical output to constant-shaped (integer keys like 1, 3) — use Davoted's actual schedule data as fixture
    - Test: buildAvailabilityMap with empty fixedSchedules object returns map without fixed entries
  </behavior>
  <action>
1. In lib/business-logic.test.js, add a new describe block "fixedSchedules from DB":
   - Create a fixture `DB_FIXED_SCHEDULES` with Davoted's schedule using STRING keys ("1", "3", "4", "5" for arkbar; "2", "3" for loveBeach) — same slot values as the FIXED_SCHEDULES constant but string keys
   - Create a fixture `CONST_FIXED_SCHEDULES` importing from the actual constant (it still exists at this point)
   - Test: call buildAvailabilityMap with each fixture for a known month, assert identical output
   - Test: call with empty object `{}`, assert no fixed entries in result

2. Run `npm test` — new tests must PASS (JS coerces integer property access on string-keyed objects).

3. In server.js, replace fetchDJs() (lines 255-268):
   - Change `supabase.from('dj_rates').select('name, rate')` to `supabase.from('djs').select('name, rate, type, active, venues, recurring_availability, fixed_schedules').eq('active', true)`
   - Map result to enriched shape: `{ name, rate: parseInt(d.rate) || 0, type: d.type, venues: d.venues || [], recurringAvailability: d.recurring_availability || {}, fixedSchedules: d.fixed_schedules || {} }`
   - Keep cache structure identical (result = { success: true, djs })

4. In server.js line 648, change `supabase.from('dj_rates').select('name')` to `supabase.from('djs').select('name').eq('active', true)` — this is inside the diagnostic endpoint and references the now-dropped dj_rates table.

5. Run `npm test` — all 49 existing tests + new tests must pass.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>fetchDJs() reads from djs table with enriched shape. Line 648 no longer references dj_rates. New test confirms DB-shaped string-key fixedSchedules works identically to constant-shaped integer-key version. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert auth to djs table + persist lockout to DB (single commit)</name>
  <files>server.js</files>
  <action>
**CRITICAL: All changes in this task MUST be in a single git commit. Partial lockout migration creates split-brain.**

1. Convert the three lockout functions (lines 81-105) to async DB-backed versions:

   - `async function checkLockout(djRow)` — takes the DJ row already fetched during auth. If `djRow.locked_until` exists and is in the future, return true. If expired, clear it (UPDATE failed_attempts=0, locked_until=null) and return false. If no djRow, return false.

   - `async function recordFailedAttempt(djName)` — query djs by ilike name to get current failed_attempts and id. Increment count. If count >= MAX_LOGIN_ATTEMPTS, set locked_until to now + LOCKOUT_DURATION_MS. UPDATE djs SET failed_attempts, locked_until WHERE id. Add try-catch — log error but do not crash.

   - `async function clearFailedAttempts(djName)` — UPDATE djs SET failed_attempts=0, locked_until=null WHERE ilike name. Add try-catch — log error but do not crash.

2. Delete the `_loginAttempts` Map (line 77). Keep MAX_LOGIN_ATTEMPTS and LOCKOUT_DURATION_MS constants.

3. Update `requireDJAuth()` (lines 350-379):
   - Replace `supabase.from('dj_pins').select('pin').ilike(...)..single()` with `supabase.from('djs').select('name, pin_hash, failed_attempts, locked_until, active').ilike('name', name.trim()).maybeSingle()`
   - Use `.maybeSingle()` NOT `.single()` (avoids PGRST116 on no match)
   - Check `if (!djRow || !djRow.active)` — reject inactive DJs
   - Pass djRow to `await checkLockout(djRow)` — MUST await (async now)
   - Compare PIN: `await bcrypt.compare(String(pin).trim(), djRow.pin_hash)`
   - On mismatch: `await recordFailedAttempt(name)` — MUST await
   - On success: `await clearFailedAttempts(name)` — MUST await

4. Update `/api/dj/login` (lines 768-793):
   - Replace `supabase.from('dj_pins').select('name, pin').ilike(...)..single()` with `supabase.from('djs').select('name, pin_hash, type, failed_attempts, locked_until, active').ilike('name', name.trim()).maybeSingle()`
   - Check `if (!djRow || !djRow.active)` — return invalid name/PIN error (do not reveal deactivated status)
   - Pass djRow to `await checkLockout(djRow)` — MUST await
   - Compare PIN: `await bcrypt.compare(String(pin).trim(), djRow.pin_hash)`
   - On mismatch: `await recordFailedAttempt(name)` — MUST await
   - On success: `await clearFailedAttempts(name)` — MUST await
   - Replace `RESIDENTS.includes(djName)` with `djRow.type === 'resident'` on line 789
   - Keep the fetchDJs() call for rate lookup (line 787-788) — rate is still needed from cache

5. Update `/api/admin/clear-lockout` (lines 1220-1225):
   - `await clearFailedAttempts(name)` — MUST await (now async)
   - Wrap in try-catch: on error return `{ success: false, error: err.message }`

6. Run `npm test` — all tests must pass. No test imports _loginAttempts so no test changes needed.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>No references to dj_pins table in requireDJAuth or /api/dj/login. The _loginAttempts Map is deleted. All lockout reads/writes go through djs.failed_attempts and djs.locked_until. All three lockout functions are async with await at every call site. Admin clear-lockout endpoint has try-catch and awaits the async function. RESIDENTS.includes() removed from /api/dj/login (replaced with djRow.type === 'resident'). All changes in a single commit.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm test` passes (all existing + new tests)
- `grep -n "dj_rates\|dj_pins" server.js` returns only comments or zero hits (no active queries to legacy tables)
- `grep -n "_loginAttempts" server.js` returns zero hits (in-memory Map removed)
- `grep -n "checkLockout\|recordFailedAttempt\|clearFailedAttempts" server.js` shows all call sites use `await`
</verification>

<success_criteria>
- fetchDJs() returns enriched DJ objects from djs table (type, recurringAvailability, fixedSchedules fields present)
- requireDJAuth and /api/dj/login authenticate against djs.pin_hash, not dj_pins
- Lockout is persisted to djs.failed_attempts and djs.locked_until — survives server restart
- All lockout function calls use await (no silent Promise-passes-truthy bugs)
- Diagnostic endpoint line 648 no longer references dj_rates
- All tests pass including new DB-shaped fixedSchedules test
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-server-cutover/08-01-SUMMARY.md`
</output>
