---
phase: 05-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/business-logic.js
  - lib/business-logic.test.js
  - server.js
  - package.json
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - "Jest tests exist and pass for availability map building logic"
    - "Jest tests exist and pass for finalization accounting calculations"
    - "Jest tests exist and pass for auto-suggest template block logic"
    - "Pure business logic functions are importable from a shared module"
  artifacts:
    - path: "lib/business-logic.js"
      provides: "Extracted pure business logic functions"
      exports: ["normalizeSlot", "parseDateKey", "makeDateKey", "pad2", "buildAvailabilityMap", "computeFinalizationReport", "getDJTemplateBlocks"]
    - path: "lib/business-logic.test.js"
      provides: "Jest test suite covering availability, accounting, auto-suggest"
      min_lines: 100
    - path: "package.json"
      provides: "Jest dev dependency and test script"
      contains: "jest"
  key_links:
    - from: "server.js"
      to: "lib/business-logic.js"
      via: "require('./lib/business-logic')"
      pattern: "require.*business-logic"
    - from: "lib/business-logic.test.js"
      to: "lib/business-logic.js"
      via: "require('./business-logic')"
      pattern: "require.*business-logic"
---

<objective>
Extract pure business logic from server.js into a testable module and add Jest test coverage for availability logic, finalization accounting, and auto-suggest template blocks.

Purpose: CLN-02 requires test coverage for the three core business logic areas. Since server.js is a monolith with no test framework, we must first extract pure functions into an importable module, then write comprehensive tests.
Output: lib/business-logic.js (extracted functions), lib/business-logic.test.js (Jest tests), updated server.js (imports from lib/).
</objective>

<execution_context>
@C:/Users/gusno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Key functions to extract from server.js — these are the contracts for testing -->

From server.js (utility functions, lines 117-137):
```javascript
const normalizeSlot = s => s ? s.replace(/[-\u2013\u2014]/g, '\u2013') : s;
const pad2 = n => String(n).padStart(2, '0');
const makeDateKey = (y, m, d) => `${y}-${pad2(m)}-${pad2(d)}`;
function parseDateKey(dateStr) { /* parses YYYY-MM-DD, D Mon YYYY, M/D/YYYY, YYYY/MM/DD */ }
```

From server.js (availability map building, lines 306-381):
```javascript
// fetchAvailability() builds an availability map: { dateKey: { slot: [djName, ...] } }
// Pure logic: given portal rows, submitted names, month params, FIXED_SCHEDULES -> map
// DB-dependent parts: fetching rows from dj_availability and dj_submissions
```

From server.js (finalization accounting, lines 1378-1417):
```javascript
// Given signoff data rows and DJ rate map, computes:
// - hours per DJ per venue (arkbar, hip, love)
// - cost = total_hours * rate
// - grandTotal, grandCost
// Pure data transformation once inputs are provided
```

From server.js (auto-suggest blocks, lines 606-659):
```javascript
function getDJTemplateBlocks(venue, dow, djName, satToggle) {
  // Returns array of contiguous slot arrays for a DJ at a venue on a day
  // Uses DIAG_FIXED_TEMPLATE (can be passed as parameter for testing)
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract business logic into lib/business-logic.js and wire into server.js</name>
  <files>lib/business-logic.js, server.js, package.json</files>
  <behavior>
    - normalizeSlot('14:00-15:00') returns '14:00\u201315:00' (en-dash)
    - normalizeSlot('14:00\u201415:00') returns '14:00\u201315:00' (em-dash to en-dash)
    - normalizeSlot(null) returns null
    - parseDateKey('2026-03-15') returns '2026-03-15'
    - parseDateKey('15 Mar 2026') returns '2026-03-15'
    - parseDateKey('3/15/2026') returns '2026-03-15'
    - parseDateKey(null) returns null
    - makeDateKey(2026, 3, 5) returns '2026-03-05'
    - buildAvailabilityMap() given empty rows returns map with only Guest DJ entries
    - buildAvailabilityMap() given portal rows and submitted names builds correct DJ-per-slot map
    - buildAvailabilityMap() injects FIXED_SCHEDULES DJs on correct days
    - computeFinalizationReport() given signoff rows computes correct hours per venue
    - computeFinalizationReport() excludes Guest DJ from report
    - computeFinalizationReport() applies last-action-wins (only 'sign' actions count)
    - computeFinalizationReport() calculates cost = total * rate correctly
    - getDJTemplateBlocks() returns contiguous blocks for Davoted on arkbar Thursday
    - getDJTemplateBlocks() returns empty array for DJ not in template
  </behavior>
  <action>
1. Run `npm install --save-dev jest` and add `"test": "jest"` to package.json scripts.

2. Create `lib/business-logic.js` that exports the following extracted pure functions:

   a. **Utility functions** (copy directly from server.js):
      - `normalizeSlot(s)` — slot string normalizer
      - `pad2(n)` — zero-pad to 2 digits
      - `makeDateKey(y, m, d)` — build YYYY-MM-DD string
      - `parseDateKey(dateStr)` — parse various date formats to YYYY-MM-DD

   b. **Constants** (copy from server.js):
      - `ALL_SLOTS`, `MONTH_NAMES`, `SHORT_MONTHS`, `RESIDENTS`
      - `FIXED_SCHEDULES`
      - `DIAG_FIXED_TEMPLATE` (the diagnostic template)

   c. **buildAvailabilityMap(params)** — extract the pure data transformation from fetchAvailability().
      Signature: `buildAvailabilityMap({ portalRows, submittedNames, month, fixedSchedules })`
      - Takes already-fetched data (no Supabase dependency)
      - `portalRows`: array of { name, date, slot, month, status } from dj_availability
      - `submittedNames`: Set of lowercase DJ names who submitted
      - `month`: string like "March 2026"
      - `fixedSchedules`: the FIXED_SCHEDULES object
      - Returns: the availability map `{ dateKey: { slot: [djNames] } }`
      - Replicate the exact logic from fetchAvailability() lines 327-381

   d. **computeFinalizationReport({ signoffRows, djRateMap })** — extract from finalize endpoint.
      - `signoffRows`: array of { name, date, slot, venue, action, timestamp } ordered by timestamp
      - `djRateMap`: object `{ lowercaseName: { name, rate } }`
      - Returns: `{ report: [...], grandTotal, grandCost }`
      - Replicate exact logic from lines 1378-1417

   e. **getDJTemplateBlocks(venue, dow, djName, satToggle, template)** — extract from server.js.
      - Add `template` as final parameter (instead of using global DIAG_FIXED_TEMPLATE)
      - Keep exact same logic from lines 606-659
      - Default `template` to DIAG_FIXED_TEMPLATE when not provided

3. Update `server.js` to `require('./lib/business-logic')` and use the imported functions instead of inline definitions:
   - Replace inline `normalizeSlot`, `pad2`, `makeDateKey`, `parseDateKey` with imports
   - Replace inline `RESIDENTS`, `ALL_SLOTS`, `MONTH_NAMES`, `SHORT_MONTHS`, `FIXED_SCHEDULES` with imports
   - In `fetchAvailability()`, call `buildAvailabilityMap()` with the fetched data instead of inline logic
   - In `getDJTemplateBlocks()`, delegate to the imported version
   - In the finalize endpoint, call `computeFinalizationReport()` with fetched data
   - Keep DIAG_FIXED_TEMPLATE and getDiagTemplateWarnings in server.js if they reference imports correctly, OR move to lib/ if cleaner

4. Run `node -c server.js` to verify no syntax errors after refactoring.
  </action>
  <verify>
    <automated>cd C:/Users/gusno/dj-roster && node -c server.js && node -c lib/business-logic.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>lib/business-logic.js exports all pure business logic. server.js imports and uses them. Both files pass syntax check. No behavioral change to the running server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Jest test suite for extracted business logic</name>
  <files>lib/business-logic.test.js</files>
  <behavior>
    - normalizeSlot converts hyphens and em-dashes to en-dashes
    - normalizeSlot handles null/undefined gracefully
    - parseDateKey handles all 4 date formats correctly
    - parseDateKey returns null for invalid input
    - makeDateKey zero-pads single-digit months and days
    - buildAvailabilityMap returns Guest DJ for every date+slot when no portal data
    - buildAvailabilityMap includes only submitted DJs (filters unsubmitted)
    - buildAvailabilityMap excludes unavailable slots
    - buildAvailabilityMap injects fixed schedule DJs on correct weekdays
    - computeFinalizationReport calculates hours per venue correctly
    - computeFinalizationReport applies last-action-wins (later 'unsign' cancels earlier 'sign')
    - computeFinalizationReport excludes Guest DJ
    - computeFinalizationReport calculates cost = total * rate
    - computeFinalizationReport returns sorted report with grandTotal and grandCost
    - getDJTemplateBlocks returns correct contiguous blocks for known DJ+venue+day
    - getDJTemplateBlocks returns empty for unknown DJ
  </behavior>
  <action>
Create `lib/business-logic.test.js` with three describe blocks:

**describe('Utility functions'):**
- Test normalizeSlot with hyphen, en-dash, em-dash, null inputs
- Test parseDateKey with YYYY-MM-DD, D Mon YYYY, M/D/YYYY, YYYY/MM/DD, null, garbage
- Test makeDateKey with single and double digit months/days
- Test pad2 with 1, 10, 0

**describe('Availability logic'):**
- Test buildAvailabilityMap with empty inputs returns Guest DJ on all slots for all days
- Test with one DJ who submitted availability for 2 slots on one day — those slots should include that DJ
- Test that a DJ who is NOT in submittedNames is filtered out
- Test that unavailable status slots are excluded from the map
- Test that FIXED_SCHEDULES DJs (Davoted) appear on their scheduled days regardless of portal submissions
- Use "March 2026" as test month (31 days, starts on Sunday)

**describe('Finalization accounting'):**
- Test with 3 signoff rows for same DJ across 2 venues — hours split correctly
- Test last-action-wins: sign then unsign for same key — should NOT count
- Test last-action-wins: unsign then sign for same key — should count
- Test Guest DJ rows are excluded
- Test cost calculation: DJ with rate 500 and 10 hours = 5000 cost
- Test venue normalization: 'Love Beach' -> 'love', 'ARKbar' -> 'arkbar', 'HIP' -> 'hip'
- Test grandTotal and grandCost aggregate correctly across multiple DJs

**describe('Auto-suggest template blocks'):**
- Test getDJTemplateBlocks('arkbar', 4, 'Davoted', 0) returns the Thursday blocks (should be 2 blocks: afternoon [14-15, 15-16, 16-17] and evening [20-21, 21-22, 22-23])
  Wait — check FIXED_SCHEDULES: Davoted arkbar Thursday (dow=4) has slots 14-15, 15-16, 20-21, 21-22, 22-23. But getDJTemplateBlocks uses DIAG_FIXED_TEMPLATE not FIXED_SCHEDULES. Pass DIAG_FIXED_TEMPLATE as parameter.
- Test getDJTemplateBlocks returns empty array for a DJ not in the template
- Test getDJTemplateBlocks for 'hip' venue returns HIP_SLOTS block for the assigned DJ

Run: `npx jest --verbose` — all tests must pass.
  </action>
  <verify>
    <automated>cd C:/Users/gusno/dj-roster && npx jest --verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All Jest tests pass. Tests cover normalizeSlot, parseDateKey, makeDateKey, buildAvailabilityMap, computeFinalizationReport, and getDJTemplateBlocks with edge cases.</done>
</task>

</tasks>

<verification>
1. `npx jest --verbose` passes all tests with 0 failures
2. `node -c server.js` passes syntax check
3. Tests cover: utility functions, availability map building, finalization accounting, auto-suggest blocks
</verification>

<success_criteria>
- Jest is installed and `npm test` runs successfully
- lib/business-logic.js exports pure functions extracted from server.js
- server.js imports from lib/business-logic.js with no behavioral change
- Test suite covers availability logic, accounting calculations, and auto-suggest
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup/05-02-SUMMARY.md`
</output>
