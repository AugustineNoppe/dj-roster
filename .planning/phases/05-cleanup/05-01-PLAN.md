---
phase: 05-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - public/roster.html
autonomous: true
requirements:
  - CLN-01
  - CLN-03

must_haves:
  truths:
    - "The reset-month endpoint does not exist in server.js"
    - "No UI button or JS function references reset-month"
    - "No commented-out code blocks or unreachable paths remain in server.js"
  artifacts:
    - path: "server.js"
      provides: "Server without reset-month endpoint or dead code"
      contains: "app.listen"
    - path: "public/roster.html"
      provides: "Roster UI without reset-month button or function"
  key_links:
    - from: "public/roster.html"
      to: "server.js"
      via: "No remaining fetch calls to /api/admin/reset-month"
      pattern: "!reset-month"
---

<objective>
Remove the dangerous reset-month endpoint and all UI references to it, then audit and remove any dead code, commented-out blocks, or unreachable paths in server.js.

Purpose: CLN-01 eliminates a production-dangerous feature that deletes all month data without safeguards. CLN-03 ensures the shipped codebase has no cruft.
Output: Clean server.js and roster.html with no dead code.
</objective>

<execution_context>
@C:/Users/gusno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove reset-month endpoint and UI references</name>
  <files>server.js, public/roster.html</files>
  <action>
1. In server.js, delete the entire reset-month block (lines 1435-1469 approximately):
   - The comment `/* == ADMIN — RESET MONTH == */`
   - The `app.post('/api/admin/reset-month', ...)` handler and its entire body
   - Leave the `/* == START == */` section and `app.listen()` intact immediately after

2. In public/roster.html, remove:
   - Line ~545: the `<button class="btn-sm btn-clear" id="btnResetMonth" onclick="resetMonthData()">Reset Month Data</button>` element
   - Lines ~1098-1115: the entire `async function resetMonthData() { ... }` function definition including the fetch call to `/api/admin/reset-month` and the success toast

3. After removal, grep the entire project for any remaining references to "reset-month", "resetMonth", or "btnResetMonth" to confirm complete removal. Ignore .planning/ directory files (those are documentation).
  </action>
  <verify>
    <automated>cd C:/Users/gusno/dj-roster && grep -rn "reset.month\|resetMonth\|btnResetMonth" server.js public/ && echo "FAIL: references remain" || echo "PASS: no references found"</automated>
  </verify>
  <done>The string "reset-month" does not appear anywhere in server.js or public/. The server starts without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and remove dead code from server.js</name>
  <files>server.js</files>
  <action>
Perform a systematic dead code audit of server.js:

1. **Commented-out code blocks**: Search for multi-line comment blocks that contain actual code (not documentation comments). The `/* == SECTION == */` headers and `// AUDIT` comments are documentation — keep those. Remove any blocks that contain commented-out function calls, variable assignments, or logic.

2. **Unreachable paths**: Check for:
   - Code after unconditional `return`, `throw`, or `res.json()`/`res.status()` within the same block
   - `if` branches that can never be true given the surrounding logic
   - Catch blocks that swallow errors silently without logging (these should at minimum log)

3. **Orphaned functions**: Verify every `function` declaration and every `const fn = ` assignment is actually referenced elsewhere in server.js. If any function is defined but never called, remove it.

4. **Orphaned constants/variables**: Check that every `const` at module scope is actually used. If a constant was only consumed by the now-deleted reset-month handler, remove it.

5. **Stale audit comments**: Phase 2 and Phase 3 added `// AUDIT (Phase X Plan Y): ...` comments. These are useful documentation — keep them. But check for any `// TODO`, `// FIXME`, `// HACK`, `// XXX` markers and resolve or remove them.

6. After cleanup, verify server.js still parses correctly by running `node -c server.js`.
  </action>
  <verify>
    <automated>cd C:/Users/gusno/dj-roster && node -c server.js && echo "PASS: syntax valid" || echo "FAIL: syntax error"</automated>
  </verify>
  <done>server.js has no commented-out code blocks, no unreachable paths, no orphaned functions or constants, and passes syntax check.</done>
</task>

</tasks>

<verification>
1. `grep -rn "reset.month\|resetMonth\|btnResetMonth" server.js public/` returns no matches
2. `node -c server.js` passes (no syntax errors)
3. `node -c public/roster.html` — N/A (HTML), but visually confirm no dangling references
</verification>

<success_criteria>
- The reset-month endpoint is completely removed from server.js
- The Reset Month Data button and resetMonthData() function are removed from roster.html
- No dead code, commented-out blocks, or unreachable paths remain in server.js
- server.js passes syntax validation
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup/05-01-SUMMARY.md`
</output>
