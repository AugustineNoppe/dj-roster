---
phase: 07-database-schema-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - scripts/drop-legacy-tables.sql
autonomous: false
requirements:
  - DB-04

must_haves:
  truths:
    - "A drop-legacy-tables.sql script exists that drops dj_rates and dj_pins"
    - "The drop script is NOT called by any other script — it is manually triggered"
    - "Human has verified all 5 Phase 7 success criteria before the drop script is run"
  artifacts:
    - path: "scripts/drop-legacy-tables.sql"
      provides: "Manual DROP TABLE statements for legacy tables"
      contains: "DROP TABLE"
  key_links:
    - from: "scripts/drop-legacy-tables.sql"
      to: "Supabase SQL Editor"
      via: "Manual paste and execute by operator"
      pattern: "DROP TABLE"
---

<objective>
Create the legacy table drop script and verify the full migration via human checkpoint.

Purpose: DB-04 requires old tables dropped only after verified cutover. This plan creates the drop script and gates execution on human verification of all Phase 7 success criteria.
Output: drop-legacy-tables.sql ready for manual execution after human sign-off.
</objective>

<execution_context>
@C:/Users/gusno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-schema-migration/07-RESEARCH.md
@.planning/phases/07-database-schema-migration/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drop legacy tables script</name>
  <files>scripts/drop-legacy-tables.sql</files>
  <action>
Create `scripts/drop-legacy-tables.sql` with:

1. A prominent header comment block stating this is a MANUAL step, not to be run until ALL 5 Phase 7 success criteria are verified:
   - SELECT COUNT(*) FROM djs returns expected DJ count with no duplicates
   - Every DJ can log in with their existing PIN after migration
   - Availability reads return same results as before migration
   - djs.recurring_availability contains correct FIXED_AVAILABILITY data
   - djs.fixed_schedules contains correct FIXED_SCHEDULES data

2. The actual DROP statements:
   ```sql
   DROP TABLE IF EXISTS dj_rates;
   DROP TABLE IF EXISTS dj_pins;
   ```

3. A verification query to confirm the tables are gone:
   ```sql
   SELECT table_name FROM information_schema.tables
   WHERE table_schema = 'public' AND table_name IN ('dj_rates', 'dj_pins');
   ```
   (Should return 0 rows after successful drop)

This file must NOT be referenced or called by migrate-djs-data.js. It is a standalone script for the Supabase SQL Editor.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>scripts/drop-legacy-tables.sql exists with DROP TABLE IF EXISTS for dj_rates and dj_pins, preceded by verification checklist comment, followed by confirmation query</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute migration and verify in live Supabase</name>
  <files>scripts/migrate-djs-schema.sql, scripts/migrate-djs-data.js, scripts/drop-legacy-tables.sql</files>
  <action>
CHECKPOINT: Human executes migration scripts and verifies results.

What was built:
- scripts/migrate-djs-schema.sql (CREATE TABLE djs)
- scripts/migrate-djs-data.js (populate from legacy tables + seed JSONB)
- scripts/drop-legacy-tables.sql (manual drop after verification)

How to verify:

1. Run the schema SQL in Supabase SQL Editor:
   - Open Supabase Dashboard > SQL Editor > New query
   - Paste contents of scripts/migrate-djs-schema.sql
   - Execute — verify all 12 columns appear in the output

2. Run the data migration script:
   - `node scripts/migrate-djs-data.js`
   - Review the console output for any WARNINGS or ERRORS
   - Confirm: total DJ count matches expectations, no duplicates reported

3. Verify Phase 7 success criteria in Supabase SQL Editor:
   - `SELECT COUNT(*) FROM djs;` — expected DJ count, no duplicates
   - `SELECT name, pin_hash FROM djs;` — all have bcrypt hashes ($2b$ prefix)
   - `SELECT name, recurring_availability FROM djs WHERE name = 'Mostyx';` — has day-of-week keys
   - `SELECT name, fixed_schedules FROM djs WHERE name = 'Davoted';` — has arkbar and loveBeach keys
   - Test at least 2 DJ logins with existing PINs via the app

4. Only after ALL above pass: run scripts/drop-legacy-tables.sql in Supabase SQL Editor
   - Verify the confirmation query returns 0 rows

Resume signal: Type "approved" after completing all verification steps, or describe any issues found.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All 5 Phase 7 success criteria verified by human, legacy tables dropped, npm test still passes</done>
</task>

</tasks>

<verification>
1. `npm test` passes (existing 49 tests unaffected)
2. scripts/drop-legacy-tables.sql exists with safety checklist and DROP statements
3. Human has verified migration results in live Supabase
</verification>

<success_criteria>
- drop-legacy-tables.sql created and committed
- Human has executed migration scripts against Supabase
- All 5 Phase 7 success criteria verified by human
- Legacy tables (dj_rates, dj_pins) dropped after verification
- DB-04 complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-schema-migration/07-02-SUMMARY.md`
</output>
